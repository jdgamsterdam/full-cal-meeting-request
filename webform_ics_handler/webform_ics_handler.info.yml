name: 'Webform ICS Handler'
type: module
description: 'Custom Webform Handler to create an ICS file on submit.'
core_version_requirement: ^10
package: Custom
dependencies:
  - webform:webform
require:
  eluceo/ical: ^2.0  # Specify the version you need
version: 1.0.0
