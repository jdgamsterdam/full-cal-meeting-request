<?php

/**
 * Implements hook_help().
 */
function webform_ics_handler_help($route_name, $route_match) {
  switch ($route_name) {
    case 'help.page.webform_ics_handler':
      return '<p>' . t('This module provides a custom Webform handler to generate an ICS file on submit.') . '</p>';
  }
}
